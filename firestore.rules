rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules: open read/write.
    // Replace with Auth-based rules before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
